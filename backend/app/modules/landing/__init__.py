"""Public / landing surface — billing, plans, the unauthenticated public_v1 API.

Mirrors `frontend/src/modules/landing/`. These submodules expose endpoints
that anonymous visitors hit (signup, plan listing, marketing-page data).
"""
