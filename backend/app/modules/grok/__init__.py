"""Grok automation back-end — profiles, jobs, files.

Mirrors `frontend/src/modules/grok/`. The submodules each own their own
router/service/schemas; this package just groups them together so the
top-level `modules/` listing matches the FE side.
"""
